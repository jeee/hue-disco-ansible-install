v12 fixes
- disco_core _enabled_lights handles lights: null safely
- dtls stream activation honors api_base_path=/api and numeric group ids for official bridge v1-compat
- avoids trying to map numeric entertainment group ids to clip/v2 uuids in v1 mode
